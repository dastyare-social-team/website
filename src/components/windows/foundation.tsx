"use client";

import WindowWrapper from "../window-wrapper";

const Foundation = () => {
  return (
    <div className="flex-1">Foundation — /.</div>
  );
};

const FoundationWindow = WindowWrapper(Foundation);

export default FoundationWindow;
