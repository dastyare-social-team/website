"use client";

import WindowWrapper from "../window-wrapper";

const Systems = () => {
  return (
    <div className="flex-1">Systems — /.</div>
  );
};

const SystemsWindow = WindowWrapper(Systems);

export default SystemsWindow;
