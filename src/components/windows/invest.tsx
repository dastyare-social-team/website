"use client";

import WindowWrapper from "../window-wrapper";

const Invest = () => {
  return (
    <div className="flex-1">Investment — /.</div>
  );
};

const InvestWindow = WindowWrapper(Invest);

export default InvestWindow;
