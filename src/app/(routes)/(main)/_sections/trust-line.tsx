import SectionWrapper from "@/components/section-wrapper";

const LandingTrustLineSectionV1 = () => {
  return (
    <SectionWrapper className="justify-center items-center md:pt-10 md:pb-10">
      <div className="max-w-md">
        <div className="text-center text-[22px] leading-8">
          Built specifically for personal brands{" "}
          <span className="text-primary bg-primary/5">
            — not stuffed into a platform built for everyone.
          </span>
        </div>
      </div>
    </SectionWrapper>
  );
};

export default LandingTrustLineSectionV1;
